<source>
  @type forward
  port 24284
  bind 0.0.0.0
</source>

<filter fluentd-test>
  @type stdout
</filter>

<match fluentd-test>
  @type forward

  # Forward
  <server>
    host 0.0.0.0
    port 24224
  </server>

</match>
